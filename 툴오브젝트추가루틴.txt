0) Bed파일 복사해서 이름바꾸기.
1)  Define - Cline_Object - 헤더파일 추가
2)  Loader에 모델컴포넌트 추가 / 오브젝트 프로토타입 추가
3)  클래스에 모델컴포넌트 해당 모델로 바꾸기 
3)  엔진에 imgui_manager.h 오브젝트프토토타입 태그추가


0) private, public에 Class파일 추가
추가할 클래스파일 안에서
1) void Tick >_int Tick 바꾸기
2) .cpp Tick에서 return 0만들기
3) Tick함수에
	if (m_Dead == true)
		return 1; 
4) Render에
#ifdef _DEBUG
	m_pSphereCom->Render();
#endif // _DEBUG

5) Client_Object.h에 헤더파일 추가
6) Loader - GamePlay함수에 프로토타입, 오브젝트파일 등록
	if (FAILED(pGameInstance->Add_Prototype(LEVEL_GAMEPLAY, TEXT("Prototype_Component_Model_House_00"),
		CModel::Create(m_pDevice, m_pDeviceContext, CModel::TYPE_NONANIM, "../Resources/Model/Environment/House/", "House_00.fbx", PivotMatrix))))
		return E_FAIL;

7)  Imgui.h에 등록한 오브젝트파일 이름 넣기(리스트에추가됨)